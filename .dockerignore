# The Android image build does not COPY repository files. Keep the build
# context tiny and avoid accidentally sending APKs, reports, logs, or keys.
**
!docker/
!docker/android/
!docker/android/Dockerfile
!.dockerignore
